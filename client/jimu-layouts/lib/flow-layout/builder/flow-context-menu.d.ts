import { React } from 'jimu-core';
import { type ContextMenuProps } from '../../builder/context-menu';
export declare function FlowContextMenu(props: ContextMenuProps & {
    isFirst: boolean;
    isLast: boolean;
}): React.JSX.Element;
