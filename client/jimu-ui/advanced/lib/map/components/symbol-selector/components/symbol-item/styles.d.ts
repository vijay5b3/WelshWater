import { type SerializedStyles, type IMThemeVariables } from 'jimu-core';
import { type ButtonSize } from 'jimu-ui';
export declare const symbolItemStyle: (theme: IMThemeVariables, btnSize: ButtonSize) => SerializedStyles;
