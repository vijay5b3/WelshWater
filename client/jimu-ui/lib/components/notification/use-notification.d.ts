import { type NotificationContextProps } from './notification-provider';
/**
 * The `useNotification` hook provides `enqueueNotification` and `closeNotification` methods to manage notifications in your application.
 *
 * ```ts
 * import { useNotification } from 'jimu-ui'
 * ```
 */
export declare const useNotification: () => NotificationContextProps;
