import { type ThemeModule } from './theme-module';
/**
 * Synchronize theme variables and theme modules between the two frameworks.
 */
export declare const useThemeSync: (module: ThemeModule) => ThemeModule;
